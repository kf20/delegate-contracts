{
    "scripts": {
        "migrate": "rmdir /Q /S build && node_modules\\.bin\\truffle migrate --reset --compile-all --network development"
    },
    "devDependencies": {
        "@openzeppelin/contracts": "^4.0.0",
        "@openzeppelin/contracts-upgradeable": "^4.0.0",
        "truffle": "^5.2.5"
    },
    "dependencies": {
        "fs-extra": "^9.1.0"
    }
}
